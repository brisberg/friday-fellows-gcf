import {FirestoreCollection} from './models';

export const configCollectionData: FirestoreCollection = {
  // id: 'config',
  docs: {
    'sync-status': {
      // id: 'sync-status',
      fields: {
        'lastSync': 1574080489155,
      }
    }
  }
}
